const messages=document.querySelector("#messages"),form=document.querySelector("#chat-form"),input=document.querySelector("#question"),status=document.querySelector("#retrieval-status");
function addMessage(text,type,sources=[]){const row=document.createElement("div");row.className=`message ${type}`;const avatar=document.createElement("div");avatar.className="avatar";avatar.textContent=type==="ai"?"F":"YOU";const content=document.createElement("div");const label=document.createElement("div");label.className="message-label";label.textContent=type==="ai"?"DIGITAL TWIN":"YOU";const body=document.createElement("div");body.textContent=text;content.append(label,body);if(sources.length){const wrap=document.createElement("div");wrap.className="sources";sources.forEach(s=>{const tag=document.createElement("span");tag.className="source";tag.textContent=s.title||s.section||s;wrap.append(tag)});content.append(wrap)}row.append(avatar,content);messages.append(row);messages.scrollTop=messages.scrollHeight}
function typing(){const row=document.createElement("div");row.className="message ai";row.id="typing";row.innerHTML='<div class="avatar">F</div><div><div class="message-label">DIGITAL TWIN</div><div class="typing">Thinking</div></div>';messages.append(row);messages.scrollTop=messages.scrollHeight}
async function ask(question){if(!question)return;addMessage(question,"user");typing();input.disabled=true;status.textContent="SEARCHING";try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question})});const data=await r.json();document.querySelector("#typing")?.remove();if(!r.ok)throw Error(data.error||"Request failed");addMessage(data.answer||"Geen antwoord ontvangen.","ai",data.sources||[]);status.textContent=data.grounded?"GROUNDED":"NO MATCH"}catch(e){document.querySelector("#typing")?.remove();addMessage("De Digital Twin is momenteel niet bereikbaar. Controleer de backend-configuratie.","ai");status.textContent="OFFLINE";console.error(e)}finally{input.disabled=false;input.focus()}}
form.addEventListener("submit",e=>{e.preventDefault();const q=input.value.trim();input.value="";ask(q)});
document.querySelectorAll("[data-question]").forEach(b=>b.addEventListener("click",()=>ask(b.dataset.question)));
